seed: 42

training:
  epochs: 20
  batch_size: 256
  learning_rate: 3e-4
  weight_decay: 0.0

model:
  player_dim: 120
  team_state_dim: 30
  auction_state_dim: 25

  num_archetypes: 8
  num_teams: 10

  embedding_dim: 16